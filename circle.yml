machine:
  python:
    version: 3.5.1

database:
  override:
    - make db_user_setup
    - make db_setup
    - python db/manage.py upgrade

test:
  override:
    - make test
    - make lint

  post:
    - codeclimate-test-reporter --file .coverage
